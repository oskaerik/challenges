WHEN NOTHING MORE TO REMOVE/ASSIGN:

Lös
Fastna
->  VALUES KOPIA (Sätt in ett possible value) (kanske typ trying=[A1, 1])
    Lös
    Fastna
    ->  VALUES KOPIA (Sätt in ett possible value)
        Lös
        Fastna
        ->  VALUES KOPIA (Inga fler possible values)
    Stryk possible value
    ->  VALUES KOPIA (Sätt in ett possible value)
        Lös
        Returnera lösning

Alternativt inga fler possible values och inga kopior = olösbart


MOTHER not solved -> solve(child, some value)
    CHILD contradiction -> return False
MOTHER not solved -> solve(child, some value)
    CHILD not solved -> solve(child, some value)
        GRANDCHILD solved -> return True